language: go

go:
  - 1.12.5

install:
  - GO111MODULE=on make deps
script:
  - GO111MODULE=on make coveralls
notifications:
  email: false
after_success:
  - test -n "$TRAVIS_TAG" && curl -sL https://git.io/goreleaser | bash
